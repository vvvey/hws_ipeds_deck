"""Deck slides — one module per slide.

Every module named `slide_*.py` in this package is picked up automatically by
`dashboards/deck.py`; there is no registry to edit. See `_shared.py` for the
contract each slide module implements and the helpers available to it.
"""
